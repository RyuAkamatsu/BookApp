import { ThemedText } from '@/components/ThemedText';
import { ThemedView } from '@/components/ThemedView';

export default function SecondAlsoNestedScreen() {
    return (
        <ThemedView>
            <ThemedText>Second Also Nested Screen</ThemedText>
        </ThemedView>
    );
}
